{
  "name": "typing-speed-backend",
  "version": "1.0.0",
  "description": "Typing speed backend using AWS SAM, API Gateway, Lambda, and DynamoDB",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1234.0"
  }
}
